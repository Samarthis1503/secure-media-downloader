services:
  - type: web
    name: secure-media-downloader
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install
    startCommand: python app.py
    plan: free 